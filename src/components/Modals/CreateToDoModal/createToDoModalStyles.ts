import {StyleSheet} from 'react-native';
import {theme} from '../../../constants/theme';

export const createToDoModalStyles = StyleSheet.create({
  input: {
    marginBottom: theme.spacing.m,
  },
});
